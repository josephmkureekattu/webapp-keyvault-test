apiVersion: v1
kind: Service
metadata:
    name: josephmkureekattuwebappkeyvaulttest
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: josephmkureekattuwebappkeyvaulttest